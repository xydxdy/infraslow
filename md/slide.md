# Update the Infraslow–Spindle Weekly Progress Presentation

Act as a senior sleep EEG researcher, scientific storyteller, and PowerPoint designer.

Update the existing presentation:

```text
infraslow/slides/Infraslow_Spindle_Progress_Update-1.pptx
```

Create the updated presentation as a new file so the original is preserved:

```text
infraslow/slides/Infraslow_Spindle_Progress_Update-2.pptx
```

## Main Objective

Add this week’s progress to the existing presentation, focusing on:

1. Infraslow feature extraction across sleep stages.
2. Individual-subject infraslow results across sleep stages.
3. N2–C3 spindle-rate group analysis.
4. Current findings and limitations.
5. The next analysis plans.

Preserve the theme, slide size, fonts, colors, layouts, and overall visual style of the existing presentation.

Do not redesign the entire deck unless a slide is clearly inconsistent or unreadable.

---

## Code to Review

Review these files to understand the current pipeline and analysis:

```text
infraslow/src/extract_features.py
infraslow/src/group_analysis.py
```

Use the actual code to explain:

* Input data.
* Sleep-stage filtering.
* EEG-channel selection.
* Infraslow spectrum calculation.
* Extracted infraslow parameters.
* Spindle-rate calculation.
* Subject grouping method.
* Statistical comparison method.
* Plot-generation logic.

Do not guess the analysis workflow when it can be determined from the code.

---

## Results to Review

Review the figures, tables, CSV files, and reports under:

```text
/scratch/users/chaisaen/infraslow_outputs/infraslow
```

Use this folder to show the results across all available sleep stages.

Also review:

```text
/scratch/users/chaisaen/infraslow_outputs/infraslow-show_individual
```

Use this folder to show individual-subject examples across all available sleep stages.

The group-analysis results were also described as being under:

```text
/scratch/users/chaisaen/infraslow_outputs/group_analysis
```

Verify whether the group-analysis outputs are actually stored in this folder or in a separate subfolder. Search for files generated by:

```text
infraslow/src/group_analysis.py
```

For example, look for filenames containing:

```text
group
comparison
assignment
N2
C3
spindle
infraslow_power
parameter
```

Do not silently assume the group-analysis path. If the expected results cannot be found, report the missing path or files clearly.

---

## Scientific Scope

The group analysis currently focuses on:

```text
Sleep stage: N2
EEG channel: C3
```

Subjects are divided into two groups using:

```text
spindle_per_min
```

The groups should be described using the actual labels used by the code, such as:

```text
low_spindle_rate
high_spindle_rate
```

The compared infraslow parameters may include:

```text
power
peak_freq_hz
peak_period_s
bandwidth_hz
auc
chromatogram_peak_area
```

Use the actual available outputs rather than claiming that every listed parameter was successfully analyzed.

---

# Required Presentation Structure

## Slide 1 — Weekly Progress Update

Use a title such as:

```text
Infraslow EEG and Sleep Spindle Analysis
Weekly Progress Update
```

Include:

* This week’s date.
* Project name.
* A short subtitle describing the current focus:

```text
Feature extraction across sleep stages and preliminary N2–C3 spindle-rate group analysis
```

Preserve the existing title-slide style.

---

## Slide 2 — This Week’s Objectives

Summarize the work completed this week:

* Consolidated the infraslow feature-extraction pipeline.
* Extracted infraslow results across sleep stages.
* Generated individual-subject diagnostic plots.
* Created the initial N2–C3 spindle-rate group analysis.
* Compared infraslow characteristics between low- and high-spindle-rate groups.
* Identified issues that require outlier handling and frequency-specific analyses.

Keep this slide concise.

---

## Slide 3 — Updated Analysis Pipeline

Create a clear workflow diagram based on:

```text
infraslow/src/extract_features.py
infraslow/src/group_analysis.py
```

The conceptual flow should be similar to:

```text
PSG/EEG data
    ↓
Sleep-stage selection
    ↓
Channel selection
    ↓
Infraslow spectrum extraction
    ↓
Background correction and peak fitting
    ↓
Infraslow summary features
    ↓
Spindle detection and spindle_per_min
    ↓
Subject-level dataset
    ↓
Low- versus high-spindle-rate grouping
    ↓
Statistical and spectral comparison
```

Show where the following features are produced:

```text
peak_freq_hz
peak_period_s
bandwidth_hz
auc
chromatogram_peak_area
spindle_per_min
spindle_per_min_SEM
```

Use concise labels rather than code-heavy descriptions.

---

## Slide 4 — Infraslow Results Across Sleep Stages

Use results from:

```text
/scratch/users/chaisaen/infraslow_outputs/infraslow
```

Show all available sleep stages, for example:

```text
Wake
N1
N2
N3
REM
```

Use the actual stages found in the results.

Present the most informative stage-comparison figure or arrange small multiples if needed.

Explain:

* Whether the infraslow spectral shape changes across stages.
* Whether the peak is consistently visible.
* Which stages appear to have stronger or more stable infraslow activity.
* Whether any stage contains visibly abnormal or highly variable results.

Do not claim statistical differences unless the results include an appropriate statistical test.

---

## Slide 5 — Individual-Subject Examples Across Stages

Use figures from:

```text
/scratch/users/chaisaen/infraslow_outputs/infraslow-show_individual
```

Show representative individual-subject results across the available sleep stages.

Select examples that demonstrate:

* A clear infraslow peak.
* A weak or uncertain peak.
* A potential outlier.
* Differences in peak position or spectral shape across sleep stages.

Use annotations or callout boxes to identify:

```text
Peak frequency
Peak period
Bandwidth
AUC
Chromatogram peak area
```

Avoid overcrowding the slide. Use no more than four representative panels unless the existing presentation layout supports more.

Clearly label each subject and sleep stage when that information is available.

---

## Slide 6 — Why Focus on N2 and C3?

Explain the current analysis scope:

```text
Sleep stage: N2
Channel: C3
```

Include the rationale supported by the code or current project decisions:

* N2 provides sufficient spindle activity for spindle-rate analysis.
* C3 is a central EEG channel commonly used for sleep-spindle evaluation.
* Restricting the first analysis to one stage and one channel reduces variability.
* The analysis is currently exploratory and will later be extended.

Do not add unsupported clinical claims.

---

## Slide 7 — N2–C3 Subject Grouping

Use outputs from:

```text
infraslow/src/group_analysis.py
```

Show the spindle-rate group distribution figure.

Present:

* Number of subjects included.
* Number in the low-spindle-rate group.
* Number in the high-spindle-rate group.
* Grouping method.
* Whether raw or transformed `spindle_per_min` was used.
* Number or percentage of uncertain assignments, if available.
* Approximate group centers or decision boundary.

Important interpretation:

The groups are defined using `spindle_per_min`; therefore, a difference in `spindle_per_min` between the groups is expected by construction and is not an independent result.

---

## Slide 8 — Infraslow Spectrum Comparison Between Groups

Use the main N2–C3 group-comparison figure generated from the plotting logic in:

```text
infraslow/src/demo_infraslow_yasa_compare.py
```

Compare:

```text
low_spindle_rate
versus
high_spindle_rate
```

The figure should retain the scientifically relevant components from the original comparison plot, where available:

* Original spectrum.
* Background or baseline spectrum.
* Corrected spectrum.
* Fitted infraslow curve.
* Peak-frequency marker.
* Peak-period information.
* Bandwidth.
* AUC.
* Chromatogram peak area.
* Spindle-rate summary.
* Group uncertainty.

Explain only the visually supported findings:

* Whether the group curves differ near the infraslow peak.
* Whether one group appears to have greater power.
* Whether peak frequency or bandwidth appears different.
* Whether variability or outliers influence the comparison.

Do not overstate preliminary findings.

---

## Slide 9 — Infraslow Parameter Comparison

Use the available statistical table and parameter-comparison plots.

Include:

```text
peak_freq_hz
peak_period_s
bandwidth_hz
auc
chromatogram_peak_area
```

Also include `power` only if the code defines a clear subject-level power metric.

Create a compact summary table containing:

```text
Parameter
Low spindle group
High spindle group
Effect direction
p-value
FDR q-value
Effect size
```

Use the actual descriptive statistics generated by the analysis.

Clearly distinguish:

* Raw p-values.
* FDR-adjusted q-values.
* Statistically significant results.
* Non-significant trends.

Do not label a result significant based only on an uncorrected p-value when FDR correction was performed.

---

## Slide 10 — Preliminary Interpretation

Summarize the current scientific interpretation.

Possible structure:

### Current evidence

* The pipeline can extract infraslow and spindle features at the subject level.
* Sleep-stage-specific spectral patterns can be visualized.
* Subjects can be separated into preliminary spindle-rate groups.
* The N2–C3 groups may differ in some infraslow characteristics.

### Current limitations

* The groups are defined from a single spindle-rate variable.
* Results may be sensitive to outliers.
* A strong concentration near approximately `0.02 Hz` may influence the group averages.
* Frequency-specific patterns may be hidden by summary metrics.
* Demographic and sleep-architecture confounders have not yet been included.
* This is an exploratory analysis and does not prove the existence of two biological subtypes.

Use the actual results to decide which statements are supported.

---

# Next Analysis Plans

## Slide 11 — Plan 1: Focused Analysis at 0.02 Hz

Present the first next-step analysis:

```text
Analyze group differences specifically at 0.02 Hz after handling outliers.
```

Proposed workflow:

1. Extract each subject’s corrected N2–C3 power at, or nearest to, `0.02 Hz`.
2. Inspect the distribution and data quality.
3. Identify extreme or invalid observations using a prespecified rule.
4. Do not remove outliers solely because they reduce significance.
5. Compare low- and high-spindle-rate groups.
6. Report descriptive statistics, effect size, confidence interval, p-value, and FDR-adjusted result where applicable.
7. Repeat the analysis with and without influential outliers as a sensitivity analysis.

Clearly state that the outlier-removal rule must be documented before evaluating the final group result.

Suggested rules may include:

* Physiologically or computationally invalid spectra.
* Non-finite values.
* Failed spectral fits.
* Extreme values based on a robust rule such as median absolute deviation.

Do not automatically use a simple arbitrary cutoff.

---

## Slide 12 — Plan 2: Frequency-by-Frequency Analysis

Present the second next-step analysis:

```text
Analyze group differences at each infraslow frequency.
```

Example frequencies:

```text
0.01 Hz
0.02 Hz
0.03 Hz
0.04 Hz
...
```

Use the actual common frequency grid produced by the pipeline.

Proposed workflow:

1. Align every subject’s N2–C3 corrected spectrum to a common frequency grid.
2. Calculate one value per subject at each frequency.
3. Compare the spindle-rate groups at each frequency.
4. Estimate group differences and confidence intervals.
5. Correct for multiple frequency comparisons.
6. Prefer cluster-based permutation testing or another appropriate method that accounts for neighboring frequencies.
7. Plot the group-difference spectrum with confidence intervals and corrected significance regions.

Do not use many uncorrected pointwise tests.

Include a conceptual example figure when no final result is available, but label it clearly as a planned analysis rather than a result.

---

## Slide 13 — Add Demographic and Sleep-Architecture Variables

Explain how Plans 1 and 2 will be extended by incorporating:

### Demographic variables

```text
Age
Gender
BMI
```

### Sleep-architecture variables

```text
TIB
SPT
WASO
TST
N1
N2
N3
REM
NREM
SOL
Lat_N1
Lat_N2
Lat_N3
Lat_REM
%N1
%N2
%N3
%REM
%NREM
SE
SME
```

Proposed analyses:

1. Compare demographic and sleep variables between the two spindle-rate groups.
2. Identify potential confounding or imbalance.
3. Analyze `0.02 Hz` power using an adjusted model.
4. Analyze frequency-specific power using adjusted models.
5. Begin with a parsimonious model such as:

```text
Power ~ spindle group + Age + Gender + BMI
```

6. Add selected sleep variables only after evaluating redundancy and collinearity.
7. Avoid including highly correlated variables in the same model, such as:

   * `TIB`, `SPT`, and `TST`
   * Absolute stage durations and their corresponding percentages
   * `NREM` together with `N1`, `N2`, and `N3`

Clarify that the adjusted analyses will test whether an infraslow–spindle association remains after accounting for relevant subject characteristics.

---

## Slide 14 — Immediate Next Steps

Finish with an actionable list:

1. Confirm the correct group-analysis output directory.
2. Review failed fits and extreme spectra.
3. Define a transparent outlier-quality rule.
4. Run the focused `0.02 Hz` analysis.
5. Run frequency-by-frequency group comparisons.
6. Apply multiple-comparison correction.
7. Merge demographic and sleep-architecture metadata.
8. Fit adjusted statistical models.
9. Extend the analysis to other sleep stages and EEG channels after validating N2–C3.

---

# Figure and Table Requirements

* Use actual result images whenever possible.
* Do not use screenshots of code.
* Crop images to remove unnecessary whitespace.
* Keep axis labels and legends readable.
* Do not stretch figures and distort their aspect ratio.
* Use vector images or high-resolution images when available.
* Rebuild tables directly in PowerPoint rather than inserting screenshots of CSV files.
* Highlight statistically important values carefully.
* Use consistent decimal precision.
* Display sample sizes.
* Include units:

  * Frequency: Hz
  * Period: seconds
  * Bandwidth: Hz
  * Spindle rate: spindles/min
* Clearly label exploratory or preliminary findings.

---

# Presentation Style

Maintain the style of:

```text
infraslow/slides/Infraslow_Spindle_Progress_Update-1.pptx
```

Requirements:

* Preserve the existing master theme and layouts.
* Use concise slide titles that communicate the main message.
* Prefer one key message per slide.
* Use large figures and limited text.
* Avoid dense paragraphs.
* Use consistent terminology:

  * `Infraslow`
  * `N2`
  * `C3`
  * `Low spindle rate`
  * `High spindle rate`
* Correct spelling and grammar throughout.
* Add slide numbers where consistent with the existing deck.
* Add concise figure captions and result-source notes.

---

# Accuracy Requirements

* Read the code and result files before writing findings.
* Do not invent numerical results.
* Do not invent sample sizes.
* Do not invent significance.
* Do not claim causality.
* Do not call the two spindle-rate groups biological subtypes without additional validation.
* Distinguish completed analyses from planned analyses.
* Clearly mark unavailable or incomplete results.
* Preserve the original scientific meaning of the plots.
* Do not modify the analysis code as part of this task unless needed only to export an existing figure or table.

---

# Expected Deliverables

Create:

```text
infraslow/slides/Infraslow_Spindle_Progress_Update-2.pptx
```

Optionally export a PDF preview:

```text
infraslow/slides/Infraslow_Spindle_Progress_Update-2.pdf
```

Also provide a concise implementation summary containing:

1. Slides added or modified.
2. Result files used on each slide.
3. Main preliminary findings included.
4. Missing or unavailable results.
5. Any assumptions made about the group-analysis output location.
6. Confirmation that the existing presentation style was preserved.
7. Confirmation that completed results and future plans were clearly separated.
